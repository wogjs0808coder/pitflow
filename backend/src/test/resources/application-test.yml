spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:h2:mem:pitflow;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE}
    username: ${TEST_DB_USERNAME:sa}
    password: ${TEST_DB_PASSWORD:}
pitflow:
  bootstrap:
    admin-email: ""
    admin-password: ""
